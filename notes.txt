.                                           Pattern types: Architectural vs Design

Architectural pattern: MVC(MODEL VIEW CONTROLLER), DI(Dependency Injection), MVP(Frontend)
⤷ desc: Skeleton of our backend. It helps us to manage and organize our project(information flow in it)

Design pattern: Middleware, Decorator 
⤷ desc: It helps us to manage only certain parts of our project, not whole.

--------

Project Standards:
⤷
    - Logging Standards:
    - Naming Standards:
        function, method(), variables => CAMEL
        class => PASCAL
        folder, file => KEBAB
        css => SNAKE
    - Error Handling Standards:

--------

PM2 BASIC COMMANDs:
⤷
    pm2 start process.config.js --env production

    pm2 ls
    pm2 start dist/server.js --name=COMFYZONE
    pm2 start "npm run start:prod" --name=COMFYZONE
    pm2 start "yarn start:prod" --name=COMFYZON-REACT
    pm2 stop id
    pm2 start id
    pm2 restart id
    pm2 monit
    pm2 start id --watch    ||  (auto-restart on changes)
    pm2 delete <id|name>
    pm2 stop all            ||  Stop all apps
    pm2 delete all
    pm2 kill

